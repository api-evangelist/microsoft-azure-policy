naftiko: 1.0.0-alpha2
info:
  label: Azure Policy REST API — Operations
  description: 'Azure Policy REST API — Operations. 1 operations. Lead operation: Azure Policy REST API List operations. Self-contained
    Naftiko capability covering one Microsoft Azure Policy business surface.'
  tags:
  - Microsoft Azure Policy
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_POLICY_API_KEY: MICROSOFT_AZURE_POLICY_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-policy-operations
    baseUri: https://management.azure.com
    description: Azure Policy REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Authorization-operations
      path: /providers/Microsoft.Authorization/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Policy REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_POLICY_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-policy-operations-rest
    port: 8080
    description: REST adapter for Azure Policy REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-authorization/operations
      name: providers-microsoft-authorization-operations
      description: REST surface for providers-Microsoft.Authorization-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Policy REST API List operations
        call: microsoft-azure-policy-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-policy-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Policy REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-policy-rest-api-list
      description: Azure Policy REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-policy-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
